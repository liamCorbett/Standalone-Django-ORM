# Standalone Django ORM Template


## Installation

With Python 3.10+ installed:

`pip install poetry`

Navigate to the root of this project

`poetry shell`

`poetry install`

`python manage.py makemigrations dbpush`

`python manage.py migrate`

`python app.py`
